{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx serve out",
    "verify": "node scripts/build.js && node scripts/verify.js"
  },
  "devDependencies": {
    "nunjucks": "^3.2.4"
  }
}
